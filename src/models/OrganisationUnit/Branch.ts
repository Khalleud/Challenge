import {OrganisationUnit} from './OrganisationUnit';

export class Branch extends OrganisationUnit {

  public unitType: String = 'Branch';
  /* constructor (name: string, config: OrganisationUnitConfig = ){
    super(name, config);
    }

  setArea(area: Area){
    this.parentUnit = area
    }
    */
}
